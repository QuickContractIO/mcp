#!/usr/bin/env node
/**
 * Binary entry point for the QuickContract MCP server (stdio transport).
 * Installed by npm as `quickcontract-mcp` or invoked via `npx @quickcontract/mcp`.
 */
import '../src/server-stdio.js';
